apiVersion: v1
kind: Service
metadata:
  name: ${service_name}
  namespace: ${namespace}
spec:
  type: ClusterIP
  ports:
  - port: ${http_port}
    targetPort: ${http_port}
    protocol: TCP
    name: http
  selector:
    app: ${app_label}
